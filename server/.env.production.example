# Production Environment Variables - Backend
# Copy this to your Render/Railway environment settings

PORT=5000
NODE_ENV=production

# MongoDB Atlas connection string (REQUIRED)
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/dbname?retryWrites=true&w=majority

# JWT Secret for authentication (REQUIRED - use a long random string)
JWT_SECRET=your_super_secret_production_key_change_me

# Google Gemini API Key (REQUIRED)
GEMINI_API_KEY=your_gemini_api_key

# Frontend URL for CORS whitelisting (REQUIRED)
FRONTEND_URL=https://your-app.vercel.app

# Additional CORS origins (comma-separated list)
# ALLOWED_ORIGINS=https://another-domain.com

# Rate Limiting
RATE_LIMIT_MAX=100
